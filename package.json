{
  "name": "lumiere-website",
  "version": "1.0.0",
  "description": "LUMIERE 2026 Film Festival Website",
  "scripts": {
    "build": "cd client && npm run build && cd ..",
    "deploy": "npm run build && firebase deploy",
    "dev": "cd client && npm run dev"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
